# anonymous fxn , fxn without name , will be useful further
#It can take any number of arguments but can only contain one expression, whose result is automatically returned
cube = lambda x: x ** 3
#also you can;t use the above defn in another vairable
# x input 
print(cube(3))